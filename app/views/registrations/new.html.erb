<% provide(:title, 'New registration') %>
<h1>New 
<% if @registration.type == 'CoachRegistration' %>
	Coach
<% else %>
	Participant
<% end %>
registration</h1>

<div class="row">
  <div class="span6 offset3">
    <%= render 'form' %>
  </div>
</div>
<%= link_to 'Back', registrations_path %>