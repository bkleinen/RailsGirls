<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @workshop.name %>
</p>

<p>
  <strong>Date:</strong>
  <%= @workshop.date %>
</p>

<p>
  <strong>Description:</strong>
  <%= @workshop.description %>
</p>

<p>
  <strong>Venue:</strong>
  <%= @workshop.venue %>
</p>

<%= link_to 'Add Coach Form', new_form_path(:type => :coach, :workshop_id => params[:id]) %>
<%= link_to 'Add Participant Form', new_form_path(:type => :participant) %>

<%= link_to 'Edit', edit_workshop_path(@workshop) %> |
<%= link_to 'Back', workshops_path %>
